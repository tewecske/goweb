// Package service contains application use-case services.
//
// Services depend on domain types and consuming-side ports, not transport,
// storage, or delivery adapters. Add a service when a use case exists; avoid
// placeholder abstractions that have no behavior.
package service
