# Templates

Store server-rendered HTML templates here.

Render with Go's `html/template`, keep authorization in handlers and services,
and escape user-controlled values through normal template execution. Templates
must not make security decisions on their own.
