# Migrations

Store ordered database schema changes here.

Use stable, sortable names such as `000001_create_users.up.sql` and pair each
forward migration with a rollback migration when the storage engine supports
safe reversal. Migrations must not contain credentials or environment-specific
secrets.
